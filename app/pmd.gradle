apply plugin: 'pmd'
pmd(type: Pmd) {
    ruleSetFiles = files("${project.rootDir}/config/pmd/pmd.xml")
    ignoreFailures = false
    ruleSets = []
    source 'src'
    include '**/*.kt'
    exclude '**/gen/**'
}